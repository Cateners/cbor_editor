# cbor_editor
An AI-generated CBOR tree editor. The root node only supports arrays and maps. Arrays only support numbers, strings, byte strings, booleans, arrays, and maps as elements. Maps only support strings as keys. I stopped here because it's already sufficient for my needs.
